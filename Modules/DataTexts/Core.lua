local T, C, L = Tukui:unpack()

local TukuiDT = T["DataTexts"]

-- local function Enable(self)
	-- self.NameColor = T.RGBToHex(unpack({0.3, 0.3, 0.3}))
	-- self.ValueColor = T.RGBToHex(unpack({0.6, 0.6, 0.6}))

-- end
-- hooksecurefunc(TukuiDT, "Enable", Enable)